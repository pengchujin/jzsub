interface:
  display_name: "JZSub"
  short_description: "最高画质下载、GPT 双语字幕与自动烧录"
  default_prompt: "Use $jzsub to download this video at maximum quality and save its cover. If subtitles exist, use next-batch to translate one compact batch at a time with the current GPT; never read the full manifest. Render and burn MiSans captions, show compact milestone progress, and continue until the delivery gate passes."
