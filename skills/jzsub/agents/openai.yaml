interface:
  display_name: "JZSub"
  short_description: "最高画质下载、GPT 双语字幕与自动烧录"
  default_prompt: "Use $jzsub to download this video at maximum quality and save its cover. When fetch exits 3 with bilingual_required, immediately translate every listed batch with the current default GPT, render and burn MiSans captions, and do not stop until the delivery gate passes. Keep long stages token-efficient and show only compact milestone progress."
